<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chat.laptop.hivego">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".login.LoginActivity"

            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.EmailLoginActivity" />
        <activity android:name=".registration.RegistrationActivity" />
        <activity android:name=".confirm_details.ConfirmActivity" />
        <activity android:name=".otpverify.OTPVerifyActivity"></activity>
        <activity
            android:name=".introduction.DefaultIntro"

            ></activity>
    </application>

</manifest>